// client.cpp
#include <iostream>
//#include "client_handler.h"  // Заголовочный файл для обработки клиентской логики

int main() {
    std::cout << "Starting chat client..." << std::endl;
    // Пример инициализации клиента
    // client.connect("server_address");  // Метод подключения к серверу

    return 0;
}
